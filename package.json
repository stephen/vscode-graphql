{
  "name": "vscode-graphql",
  "author": "Stephen Wan <stephen@stephenwan.com>",
  "license": "MIT",
  "repository": "git@github.com:stephen/vscode-graphql.git",
  "displayName": "vscode-graphql",
  "description": "",
  "version": "0.0.1",
  "publisher": "stpn",
  "engines": {
    "vscode": "^1.13.0"
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "watch:compile": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "categories": ["Languages"],
  "contributes": {
    "commands": [
      {
        "command": "extension.sayHello",
        "title": "Hello World"
      }
    ],
    "languages": [
      {
        "id": "graphql",
        "aliases": ["GraphQL", "graphql"],
        "extensions": [".graphql"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "graphql",
        "scopeName": "source.graphql",
        "path": "./syntaxes/graphql.tmLanguage.json"
      }
    ]
  },
  "activationEvents": ["onLanguage:graphql"],
  "main": "./bin/extension",
  "dependencies": {
    "graphql": "^0.10.3",
    "vscode": "^1.1.10"
  },
  "devDependencies": {
    "prettier": "^1.5.3"
  }
}
